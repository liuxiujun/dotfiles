[core]
    # Linux 下使用系统路径中的 nvim
    editor = nvim

[credential]
    # Linux 推荐使用 libsecret（需安装）
    # helper = libsecret

[push]
    # 在 Linux 上推送时自动设置上游
    autoSetupRemote = true

[color]
    # 终端支持真彩色（可选）
    ui = auto
